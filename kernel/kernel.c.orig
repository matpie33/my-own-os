#include "../cpu/isr.h"
#include "../cpu/timer.h"
#include "../drivers/screen.h"
#include "../drivers/keyboard.h"
#include "../libc/strings.h"
<<<<<<< Updated upstream
#include "../libc/hex_to_string.h"
=======
#include "../libc/function.h"
#include "../libc/hex_to_string.h"
#include "kernel.h"
>>>>>>> Stashed changes

void start() { 
	println("Kernel is working.");
//	clear_screen();
	println("Welcome to my os.");
	println("Hex to string: ");
	hex_to_string(0x25);
	isr_install();
	asm volatile("sti");
//
//	init_timer(50);
	init_keyboard();
//
//	__asm__ __volatile__("int $2");
//	__asm__ __volatile__("int $3");
}        

void test_method(registers_t regs){
	UNUSED(regs);
	// TODO implement hex to string method
	hex_to_string(best_video_mode.y_cur_max);
}

void user_input(char *input) {
    if (strcmp(input, "END") == 0) {
        print("Stopping the CPU. Bye!\n");
        __asm__ ("hlt");
    }
    print("You said: ");
    println(input);
}
